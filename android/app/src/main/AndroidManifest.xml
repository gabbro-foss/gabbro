<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />
    <!-- R-02 (AI_SECURITY_AUDIT_REVIEW.md): the vault and settings live in
         app-private storage and must never leave the device via the Android
         backup framework — neither Auto Backup to Google Drive nor
         device-to-device migration. Intent is declared at every layer the OS
         (and OEM transfer tools) may consult: the blanket flag, the API 31+
         rules, and the API <= 30 rules. User-driven export/backup via SAF to
         shared storage is unaffected. Guarded by BackupConfigRobolectricTest. -->
    <application
        android:label="gabbro"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Intercept YubiKey NFC OTP URL in all contexts (foreground and
                 background) to prevent the NDEF record from opening the browser.
                 launchMode="singleTop" routes these to onNewIntent, which
                 swallows them. -->
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="https" android:host="my.yubico.com"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="https" android:host="demo.yubico.com"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!--
            GabbroAutofillService — invoked by the Android OS when the user
            focuses a login field in any app. Requires BIND_AUTOFILL_SERVICE,
            which is a system-level permission (not a <uses-permission> entry).
        -->
        <service
            android:name=".GabbroAutofillService"
            android:exported="true"
            android:permission="android.permission.BIND_AUTOFILL_SERVICE">
            <intent-filter>
                <action android:name="android.service.autofill.AutofillService" />
            </intent-filter>
            <meta-data
                android:name="android.autofill"
                android:resource="@xml/autofill_service_config" />
        </service>

        <!--
            UnlockActivity — shown by the OS when a fill request arrives and
            the vault is locked. A GabbroUnlockHostActivity subclass that runs
            the autofillUnlockMain entrypoint (reused UnlockScreen: vault picker,
            passphrase, YubiKey, biometric) and returns the requested credential
            to the OS once the vault session is unlocked.
        -->
        <activity
            android:name=".UnlockActivity"
            android:exported="false"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize" />

        <!--
            SaveActivity — the autofill SAVE counterpart to UnlockActivity. Launched
            by GabbroAutofillService.onSaveRequest after a login is submitted. Runs the
            autofillSaveMain entrypoint (unlock if needed, then the SaveConfirmScreen)
            and writes the new/changed login via the Dart bridge.
        -->
        <activity
            android:name=".SaveActivity"
            android:exported="false"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize" />

        <!--
            AutofillSettingsActivity — reachable from Android Settings →
            Autofill service → Settings (via autofill_service_config.xml).
            Renders the Flutter autofill settings route.
        -->
        <activity
            android:name=".AutofillSettingsActivity"
            android:exported="true"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required for url_launcher to open https/http URLs in the system browser -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>
    </queries>
</manifest>
