# Robolectric runs tests against an emulated Android SDK level. compileSdk is 36,
# but Robolectric 4.13 only ships android-all jars up to API 34, so pin the test
# runtime here rather than per-class @Config(sdk = ...).
sdk=34
